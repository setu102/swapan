
# 🚀 VPS (Ubuntu/Nginx) হোস্টিং গাইড

আপনার অ্যাপটি VPS-এ হোস্ট করার জন্য নিচের ধাপগুলো অনুসরণ করুন। এটি **Puter.js** এবং **Gemini API** উভয় মোডেই কাজ করে।

## ১. সার্ভার সেটআপ
```bash
sudo apt update
sudo apt install nodejs npm nginx -y
```

## ২. প্রোজেক্ট ক্লোন ও ডিপেন্ডেন্সি
```bash
cd /var/www/html
# আপনার কোড এখানে রাখুন
npm install
```

## ৩. বিল্ড অপশন (আপনার পছন্দ অনুযায়ী)

### অপশন এ: শুধুমাত্র Puter.js ব্যবহার করলে (কোনো Key লাগবে না)
আপনি যদি এপিআই কী ছাড়াই সম্পূর্ণ ফ্রি মোডে চালাতে চান:
```bash
npm run build
```

### অপশন বি: স্মার্ট ফিচার (Search/Map) সচল করতে (Gemini API Key সহ)
আপনি যদি ট্রেনের লাইভ ট্র্যাকিং এবং ম্যাপের সঠিক সার্চ রেজাল্ট চান, তবে জেমিনি কী ব্যবহার করা ভালো:
```bash
export API_KEY="আপনার_জেমিনি_কী"
npm run build
```

## ৪. Nginx কনফিগারেশন
`/etc/nginx/sites-available/default` ফাইলে নিচের অংশটি দিন:
```nginx
server {
    listen 80;
    root /var/www/html/dist; 
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

## ৫. রিস্টার্ট
```bash
sudo systemctl restart nginx
```

**নোট:** অ্যাপটি কোড লেভেলে এমনভাবে তৈরি যে এটি প্রথমে চেক করবে আপনার কাছে **API_KEY** আছে কিনা। থাকলে এটি পাওয়ারফুল জেমিনি এআই ব্যবহার করবে, আর না থাকলে এটি সম্পূর্ণ ফ্রিতে **Puter.js** এর মাধ্যমে কাজ চালিয়ে নেবে।
